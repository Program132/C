#include "str.h"
#include <iostream>
#include <string>

using namespace N;

using string = std::string;

int st::length(string text)
{
	int a = text.length();
	return a;
}

int st::size(string text)
{
	int a = text.size();
	return a;
}